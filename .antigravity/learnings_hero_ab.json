{
  "reminders": [
    "When moving the Dynamic Hero Strategy to production, update the fetch URLs in src/hooks/useHeroVariant.js to point to the actual Java Spring Boot Growth Engine endpoints instead of localhost:8080 mock URLs."
  ]
}
